# Copy to .env.local (never commit real keys).

# --- Notion content (AI news + LinkedIn posts) ---
# Until these are set, the site serves bundled sample data.
# Key: internal integration secret from notion.so/profile/integrations.
# The integration must be given access to BOTH databases
# (database page -> ... menu -> Connections -> add the integration).
NOTION_API_KEY=
# "AI News English Posts" database (fed daily by the AI Curriculum pipeline)
NOTION_NEWS_DB_ID=fa9d00d535bc49f4b354ffae079697b8
# "TFA Website - LinkedIn Posts" database
NOTION_LINKEDIN_DB_ID=25e9fdbe77d34a889dc894574c319833

# --- Booking enquiries ---
# Resend (resend.com) API key. Until set, enquiries are logged server-side only.
RESEND_API_KEY=
# From-address on a Resend-verified domain. The account's verified domain is
# instituteof.ai; switch to enquiry@timfrankandersen.com once that domain is
# verified in Resend after the DNS cutover.
ENQUIRY_FROM=enquiry@instituteof.ai
